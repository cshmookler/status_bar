// Standard includes
#include <string>

// Local includes
#include "../constants.hpp"
#include "../status.hpp"

namespace sbar {

std::string Fields::get_camera_status() {
    return sbar::null_str;
}

} // namespace sbar
